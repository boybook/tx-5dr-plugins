This repository publishes plugin marketplace assets to the dedicated Aliyun OSS
bucket for downloadable product artifacts, under the `plugins/` prefix. The
download domain is bound directly to OSS, so deployment only uploads objects
and does not perform any CDN cache refresh.
